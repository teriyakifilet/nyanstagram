<div class="contents-wrapper">
  <div class="cat-information-wrapper">
    <div class="cat-information-icon">
      <%= image_tag @cat.icon, class: "cat-icon" %>
    </div>
    <div class="cat-information-detail">
      <ul class="cat-information-list">
        <li><h2><%= @cat.cat_name%>ちゃん</h3>
        <li><%= render 'relationships/follow', cat: @cat %>
        <li><%= link_to "#{@cat.user.user_name}さん", user_path(@cat.user) %>ちのねこちゃんです
        <li>品種 <%= @cat.cat_breed.name %>
        <li>性別 <%= @cat.cat_sex.name %>
        <li>年齢 <%= @cat.cat_age %>さい
        <% if user_signed_in? && current_user.id == @cat.user_id%>
          <li><h4><%= link_to "編集する", edit_cat_path(@cat) %></h4>
        <% end %>
      </ul>
    </div>
  </div>
  <div class="cat-photos-wrapper">
    <div class="cat-photos-title">
      <h3><%= @cat.cat_name %>ちゃんのお写真</h3>
    </div>
    <% if @photos.present? %>
      <div class="cat-photos">
        <% @photos.each do |photo| %>
          <div class="cat-photo">
            <%= link_to photo_path(photo) do %>
              <%= image_tag photo.cat_photo, class: "cat-image"%>
            <% end %>
            <%= photo.detail%>
            <div class="like-btn">
              <%= render 'likes/like', photo: photo %>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      まだお写真が投稿されていないようです。
      <% if @cat.user_id == current_user.id %>
        <%= link_to "写真を投稿する", new_photo_path %>
      <% end %>
    <% end %>
  </div>
</div>